# Runtime Overlays

Runtime overlays are copied into `.praxis/overlays/` when a foundation is applied.

They are intended to hold optional extension seams that can evolve independently from the base profile.
