# 能力：用户查询 (User Query)

## 新增需求

### 需求：按状态过滤用户
系统应该允许通过可选的 `status` 参数过滤用户列表。

#### 场景：成功按状态过滤
- **当** 调用查询接口并传入 `status=ACTIVE`
- **则** 系统仅返回状态为 `ACTIVE` 的用户列表

#### 场景：不传状态参数时返回全部
- **当** 调用查询接口且不传 `status` 参数
- **则** 系统返回所有状态的用户列表
