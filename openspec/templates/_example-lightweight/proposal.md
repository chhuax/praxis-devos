# 变更：用户查询接口增加状态过滤

## 动机
目前用户查询接口返回所有状态的用户，前端需要根据用户状态（如：启用、禁用）进行过滤显示，以提高用户体验。

## 变更内容
- 在用户查询接口中增加可选的 `status` 参数。
- 当传入 `status` 时，仅返回匹配该状态的用户；不传时保持现有行为，返回所有用户。

## 影响范围
- 受影响的规范：`user-query`
- 受影响的代码：`UserService.java`, `UserController.java`
