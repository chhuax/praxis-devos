name: Install Smoke

on:
  push:
    branches:
      - main
      - 'feat/**'
  pull_request:

concurrency:
  group: install-smoke-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  install-smoke:
    name: Install Smoke (${{ matrix.os }} / ${{ matrix.scenario }} / ${{ matrix.command_path_mode }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            scenario: codex
            command_path_mode: normal
          - os: macos-latest
            scenario: codex
            command_path_mode: normal
          - os: windows-latest
            scenario: codex
            command_path_mode: normal
          - os: ubuntu-latest
            scenario: opencode
            command_path_mode: normal
          - os: windows-latest
            scenario: opencode
            command_path_mode: normal
          - os: ubuntu-latest
            scenario: claude
            command_path_mode: normal
          - os: windows-latest
            scenario: claude
            command_path_mode: normal
          - os: windows-latest
            scenario: claude
            command_path_mode: quoted-windows-space

    runs-on: ${{ matrix.os }}
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          package-manager-cache: false

      - name: Show tool versions
        run: |
          node --version
          npm --version
          git --version

      - name: Pack tarball
        run: npm pack --json > pack.json

      - name: Export pack filename
        shell: bash
        run: |
          PACK_FILE="$(node -e "process.stdout.write(JSON.parse(require('node:fs').readFileSync('pack.json','utf8'))[0].filename)")"
          echo "PACK_FILE=$PACK_FILE" >> "$GITHUB_ENV"

      - name: Run install smoke
        run: node test/install-smoke-cli.mjs --package "${{ env.PACK_FILE }}" --scenario "${{ matrix.scenario }}" --command-path-mode "${{ matrix.command_path_mode }}"
