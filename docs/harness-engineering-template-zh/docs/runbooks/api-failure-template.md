# Runbook：API 故障

## 触发条件

当公共 API 接口出现异常 4xx、5xx、延迟激增或响应结构不匹配时，使用本 runbook。

## 触发阈值

- <填写错误率阈值>
- <填写延迟阈值>

## 输入信息

- 接口路径
- 请求 ID
- 时间戳
- 环境
- 调用方身份

## 观测入口

- 监控面板：<填写 API 面板链接或名称>
- 日志入口：<填写日志查询入口>
- 追踪入口：<填写 trace 查询入口>

## 检查步骤

1. 在 `contracts/openapi/public.yaml` 中确认接口契约。
2. 确认请求结构是否符合契约。
3. 检查认证与权限映射。
4. 检查最近契约变更或部署变更。
5. 检查下游依赖健康状态。

## 诊断命令

```bash
<填写接口探测命令>
<填写日志查询命令>
```

## 恢复动作

- 如果存在配置漂移，先修正错误配置。
- 如果最近版本存在不兼容变更，必要时回滚最新部署。
- 如果契约发生变化，补充并发布迁移说明。

## 回滚条件

- <填写何种情况下允许回滚最新版本>

## 回滚步骤

1. <填写回滚步骤>
2. <填写回滚后验证步骤>

## 验证方式

- 用已知正确请求重新验证
- 确认响应结构与契约一致
- 确认没有新增鉴权回归

## 升级条件

- <填写何种情况下升级给平台负责人>
- <填写何种情况下升级给依赖方团队>

## 事后补充

- 如契约已变化，更新 `contracts/openapi/public.yaml`
- 如调用方式已变化，补充 `docs/guides/` 中的迁移说明
- 如错误模型已变化，更新 `contracts/errors/errors.yaml`

