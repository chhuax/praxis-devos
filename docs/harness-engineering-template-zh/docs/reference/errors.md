# 错误码参考文档

## 事实源

- `contracts/errors/errors.yaml`

## 规则

- 调用方应依赖稳定错误码，而不是直接依赖原始错误消息文本。
- 是否可重试应依据明确的错误语义和状态码定义来判断。

## 错误码

### `AUTH_UNAUTHORIZED`

- HTTP 状态码：`401`
- 是否可重试：否
- 含义：调用方未完成认证

### `VALIDATION_FAILED`

- HTTP 状态码：`400`
- 是否可重试：否
- 含义：请求参数或请求体不合法

