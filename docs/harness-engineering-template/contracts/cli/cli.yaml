commands:
  - name: sync
    description: Synchronize local state with remote state
    arguments:
      - name: --project-dir
        required: true
        type: string
    outputs:
      - code: 0
        meaning: Success
      - code: 1
        meaning: Validation or runtime failure

