# OpenSpec Changes

Use this directory to store active change artifacts such as:

- `proposal.md`
- `design.md`
- `tasks.md`
- change-local notes that must later be synced back to contracts and docs

Rule:

- temporary change artifacts do not replace `contracts/` as the source of truth
- before archive or completion, sync stable outcomes back into contracts and persistent docs

